# 🧠 Lógica de Programação em C

Esta pasta contém exercícios focados na sintaxe e nos conceitos fundamentais da linguagem C. Cada arquivo isola um conceito específico para servir de guia de consulta rápida.

## 📚 Índice de Estudos

### 1. Fundamentos
- **Tipos e Variáveis:** `Tipos de dados Primitivos`, `Variaveis` e `Modificadores`.
- **Entrada e Saída:** `Entrada e Saida de dados`.
- **Operadores:** `Aritméticos`, `Atribuição`, `Lógicos` e `Ternário`.

### 2. Estruturas de Decisão
Controle de fluxo condicional:
- **Básico:** `Estrutura Condicional Simples e Composta`.
- **Avançado:** `Encadeada`, `Aninhada`.
- **Seleção:** `Menus Interativos com Switch`.

### 3. Estruturas de Repetição (Loops)
Automação de tarefas:
- **Laços:** `for`, `while`, `do-while`.
- **Controle:** `Loops com o uso de Continue e Break`.
- **Complexidade:** `Loops aninhados e múltiplas variáveis`.

### 4. Estruturas de Dados
- **Vetores (Arrays):** `Manipulação básica` e `operações matemáticas`.
- **Matrizes:** `Criação de grids`, `busca condicional` e `modificação de elementos`.
- **Integração:** `Operações combinadas entre matrizes e vetores`.

### 5. Modularização
- **Funções e Procedimentos:** `Organização do código`.
- **Recursividade:** `Chamadas recursivas`.

---

## 🚀 Como Executar
Para rodar qualquer código desta pasta via terminal com GCC:

```bash
# Compilar
gcc NomeDoArquivo.c -o saida

# Executar (Linux/Mac)
./saida

# Executar (Windows)
./saida.exe